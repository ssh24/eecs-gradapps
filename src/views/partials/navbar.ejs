<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                    <a class="navbar-brand" href="#"><img src="/image/logo.png"/></a>
                </div>
                <div id="navbar" class="navbar-collapse collapse">
                    <ul class="nav navbar-nav navbar-right">
                            <% if (user && role && fullname) { %>
                                <li><a id="logged-in-as" style="color:white">Logged in as: <%= fullname %></a></li>
                                <li><a href="#" data-toggle="modal" data-target="#filterModal">Filter<span class="glyphicon glyphicon-pencil"></span></a></li>
                                <li class="dropdown"><a id="role-selected" href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Role: <%= role %><span class="caret"></span></a>
                                    <% if (roles.length > 1) { %>
                                        <ul class="dropdown-menu">
                                                <% if (role === 'Admin') { %>
                                                    <% if (roles.includes('Committee Member')) { %>
                                                        <li><a id="switch-to-committee" href="/roles/committee">Committee Member</a></li>
                                                    <% } %>
                                                    <% if (roles.includes('Professor')) { %>
                                                        <li><a id="switch-to-professor" href="/roles/professor">Professor</a></li>
                                                    <% } %>
                                                <% } %>
                                                <% if (role === 'Committee Member') { %>
                                                    <% if (roles.includes('Admin')) { %>
                                                        <li><a id="switch-to-admin" href="/roles/admin">Admin</a></li>
                                                    <% } %>
                                                    <% if (roles.includes('Professor')) { %>
                                                        <li><a id="switch-to-professor" href="/roles/professor">Professor</a></li>
                                                    <% } %>
                                                <% } %>
                                                <% if (role === 'Professor') { %>
                                                    <% if (roles.includes('Admin')) { %>
                                                        <li><a id="switch-to-admin" href="/roles/admin">Admin</a></li>
                                                    <% } %>
                                                    <% if (roles.includes('Committee Member')) { %>
                                                        <li><a id="switch-to-committee" href="/roles/committee">Committee Member</a></li>
                                                    <% } %>
                                                <% } %>
                                            </ul>
                                    <% } %>
                                </li>
                            <% } %>
                            <% if (user) { %>
                                <li><a id="logout-btn" href="/logout">Sign out &raquo;</a></li>
                            <% } %>
                    </ul>
                </div>
                <!--/.navbar-collapse -->
            </div>
        </nav>
